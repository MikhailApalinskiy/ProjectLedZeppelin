services:
  - type: web
    name: ledzeppelin
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: JAVA_TOOL_OPTIONS
        value: -XX:MaxRAMPercentage=75.0 -Duploads.dir=/data/uploads
    disk:
      name: data
      mountPath: /data
      sizeGB: 2